import React from "react";
import Products from "./Products";

const Home = (props) => {
  return (
    <div className="container">
      <Products></Products>
    </div>
  );
};

export default Home;
